# secrets & runtime state — never commit these
.env
.env.*
!.env.example
!.env.selftest
data/
logs/
*.session
*.session-journal

# the cloud workflow lives in .github/ and MUST be uploaded
!.github/
!.github/workflows/

# python
.venv/
__pycache__/
*.py[cod]
